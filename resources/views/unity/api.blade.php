using System;
public class WEBAPI {

    private static WEBAPI instance=null;
    
    public static WEBAPI ins(){
        if(instance==null){
            //初期化コード
        }
        return instance;
    }
    
    
    private static exec(Proc,params,Success s,Error e){
        
        StartCoroutine(
        
        );
    }
    
    
    
    @foreach($api_list as $api)
    
    @endforeach

}